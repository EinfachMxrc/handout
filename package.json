{
  "name": "slide-handout-root",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter web dev\" \"pnpm --filter powerpoint-addin dev\" \"pnpm --filter convex-backend dev\"",
    "dev:web": "pnpm --filter web dev",
    "dev:addin": "pnpm --filter powerpoint-addin dev",
    "dev:convex": "npx convex dev --run init:init",
    "seed": "npx convex run init:init",
    "setup": "node scripts/setup-env.mjs",
    "build": "pnpm --filter reveal-engine build && pnpm --filter shared build && pnpm --filter web build && pnpm --filter powerpoint-addin build",
    "test": "pnpm --filter reveal-engine test",
    "test:all": "pnpm -r test",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "dependencies": {
    "convex": "^1.36.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
