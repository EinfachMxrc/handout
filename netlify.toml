[build]
  base = "apps/web"
  command = "pnpm --filter web build"
  publish = ".next"

[build.environment]
  # NEXT_PUBLIC_CONVEX_URL is intentionally exposed to the client (required by Convex)
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_CONVEX_URL"

[[plugins]]
  package = "@netlify/plugin-nextjs"
